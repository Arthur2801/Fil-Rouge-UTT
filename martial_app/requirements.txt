# ==============================================================================
# REQUIREMENTS.TXT - Dépendances pour le déploiement sur Streamlit Cloud
# ==============================================================================
# Application: Assistant Intelligent Dealabs (Recherche RAG)
# Python: 3.9+ (testé avec Python 3.13)
# Dernière mise à jour: Janvier 2026
# ==============================================================================

# --- FRAMEWORK WEB ---
# Streamlit pour l'interface utilisateur web
streamlit>=1.30.0

# --- BASE DE DONNÉES ---
# MongoDB avec support des connexions Atlas (srv://)
pymongo[srv]>=4.6.0
# Résolution DNS pour les connexions MongoDB Atlas
dnspython>=2.4.0

# --- GESTION DE L'ENVIRONNEMENT ---
# Chargement des variables d'environnement depuis .env
python-dotenv>=1.0.0

# --- INTELLIGENCE ARTIFICIELLE & NLP ---
# LangChain pour l'orchestration LLM
langchain>=0.1.0
langchain-core>=0.1.0
langchain-community>=0.0.20

# Intégration GROQ pour le LLM
langchain-groq>=0.0.1

# Intégration HuggingFace pour les embeddings
langchain-huggingface>=0.0.1

# Modèles de sentence embeddings (all-MiniLM-L6-v2)
sentence-transformers>=2.2.0

# --- DEEP LEARNING ---
# PyTorch CPU-only (léger pour Streamlit Cloud)
# --extra-index-url permet d'utiliser l'index CPU de PyTorch
--extra-index-url https://download.pytorch.org/whl/cpu
torch>=2.0.0

# --- TRAITEMENT D'IMAGES ---
# Pillow pour charger et afficher le logo
Pillow>=10.0.0

# --- DÉPENDANCES SCIENTIFIQUES ---
# NumPy pour les opérations numériques (requis par sentence-transformers)
numpy>=1.24.0

# Scikit-learn pour les métriques de similarité
scikit-learn>=1.3.0

# --- SÉCURITÉ & RÉSEAU ---
# Certifi pour les certificats SSL (connexions HTTPS/MongoDB Atlas)
certifi>=2023.7.22

# ==============================================================================
# NOTES DE DÉPLOIEMENT :
# ==============================================================================
# 1. Streamlit Cloud utilise pip pour installer les dépendances
# 2. Le fichier .env n'est PAS committé (utiliser Streamlit Secrets)
# 3. Variables d'environnement requises:
#    - MONGO_URI: URI de connexion MongoDB Atlas
#    - GROQ_API_KEY: Clé API GROQ pour le LLM
# 4. PyTorch CPU est utilisé pour réduire la taille (pas de GPU sur Streamlit)
# 5. MongoDB Atlas doit avoir:
#    - Un index vectoriel nommé "vector_index" sur le champ "embedding"
#    - Une collection "deals" dans la base "deals_db"
# ==============================================================================















